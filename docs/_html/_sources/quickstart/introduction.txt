Introduction
----------------

The aim of this section is to show the user how to run a single simulation in APySPAM, and give an
outline of what the code outputs during the run should look like as well as how the algorithm saves
the simulation results. Any description of the underlying processes and 

There are two ways to run APySPAM: as a package, inputting the parameters in the CLI, or by directly
editing the parameters in ``SetupUtil.py``. Both will be described in this section.